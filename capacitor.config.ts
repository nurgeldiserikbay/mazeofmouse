import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thelightcome.mazeofmouse',
  appName: 'Maze Of Mouse',
  webDir: 'docs',
  server: {
    androidScheme: 'https'
  }
};

export default config;
